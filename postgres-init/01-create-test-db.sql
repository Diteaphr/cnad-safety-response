-- Automatically creates the test database alongside the main database.
-- This script runs only on first container startup (when postgres_data volume is empty).
CREATE DATABASE employee_safety_test;
