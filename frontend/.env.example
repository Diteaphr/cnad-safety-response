# 選項 A — 留空（不要寫這行或設為空）：與前端同源，開發時由 Vite 把 `/api` 轉到本機 CNAD 後端。
# 預設 `http://127.0.0.1:8001`（若 8000 被其他專案占用，請在 `.env.local` 覆寫）：
# CNAD_API_PROXY_TARGET=http://127.0.0.1:8001
#
# 選項 B — 直連後端（瀏覽器會對該 origin 發請求，後端已開 CORS）：
# VITE_API_URL=http://localhost:8000
#
#（請勿在結尾加 `/api`，程式會自動去除誤設。）

