# 選項 A — 留空（不要寫這行或設為空）：與前端同源，開發時由 Vite 把 `/api` 轉到本機 8000；
#         Docker 前端映像則由 nginx 轉到 `backend` 服務。
#
# 選項 B — 直連後端（瀏覽器會對該 origin 發請求，後端已開 CORS）：
# VITE_API_URL=http://localhost:8000
#
#（請勿在結尾加 `/api`，程式會自動去除誤設。）

