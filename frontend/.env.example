# 選項 A — 留空（不要寫這行或設為空）：與前端同源，開發時由 Vite 把 `/api` 轉到 **本機 8000**
# （若要改後端位址再在下面設 CNAD_API_PROXY_TARGET，勿結尾 /；或使用選項 B 直連。）
#
# 選項 B — 直連後端（瀏覽器會對該 origin 發請求，後端已開 CORS）：
# VITE_API_URL=http://localhost:8000
#
#（請勿在結尾加 `/api`，程式會自動去除誤設。）

